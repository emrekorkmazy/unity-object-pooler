# unity-object-pooler
A Unity object pooling system that simplifies managing reusable game objects. Features include ScriptableObject-based configuration for prefabs, a static factory for easy access, and optional VContainer dependency injection integration. Designed for performance and modularity in Unity projects.
